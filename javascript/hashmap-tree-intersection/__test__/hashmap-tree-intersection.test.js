
const hashmap = require('../hashmap-tree-intersection');




describe('tree intersection', () => {

  it('when given no inputs, expect to throw', () => {
    expect(() => hashmap()).toThrow();
  });

});
